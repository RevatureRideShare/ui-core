pipeline{
    agent any
    triggers {
        cron('H H/6 * * 1-5')
    }
    stages{
        stage('View package versions'){
            steps{
                sh '''
                pwd
                node -v
                npm -v
                mvn -v
                '''
            }
        }
    }
}