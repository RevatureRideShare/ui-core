#!/bin/bash

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "I am building RideShare Angular"
		    . ~/.nvm/nvm.sh
                    ng build
                '''
            }
        }
    }
}